using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum InputKeys {
    up, left, down, right, enter, back, jump, empty, action
}

public enum Status{
    main, pause, level
}

public enum Scenes{
    main, tutorial, level1, testing
}